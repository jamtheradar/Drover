<Window x:Class="Drover.App.Views.CommandPalette"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual"
        Width="600" Height="400"
        Deactivated="Window_Deactivated"
        PreviewMouseWheel="Window_PreviewMouseWheel">
    <Border Background="#2d2d30"
            BorderBrush="#007acc"
            BorderThickness="1"
            CornerRadius="4">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <TextBox x:Name="FilterBox"
                         Padding="12,10"
                         FontSize="14"
                         Background="#1e1e1e"
                         Foreground="#ddd"
                         BorderThickness="0,0,0,1"
                         BorderBrush="#333"
                         TextChanged="FilterBox_TextChanged"
                         PreviewKeyDown="FilterBox_PreviewKeyDown" />
                <TextBlock x:Name="PlaceholderHint"
                           Text="Type to filter · &gt; send to one tab · &gt;&gt; broadcast to all"
                           Foreground="#666"
                           FontSize="13"
                           Padding="14,11"
                           IsHitTestVisible="False" />
            </Grid>
            <ListBox Grid.Row="1"
                     x:Name="Results"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="#ddd"
                     FontSize="13"
                     MouseDoubleClick="Results_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Label}" />
                            <TextBlock Grid.Column="1" Text="{Binding Category}"
                                       Foreground="#888" Margin="12,0,0,0" FontSize="11" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="12,6" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>
    </Border>
</Window>
